# 创建子包
请复制`doc/创建需求说明模板.md`为基础，
在与目标最匹配的落地组件目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该目标的版本。

## 当前目标：


# 复刻
请复制`doc/像素级复刻需求说明模板.md`为基础，
在与复刻目标最匹配的落地目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该复刻目标的版本。

## 当前复刻目标：


# 迁移
请复制`doc/迁移需求说明模板.md`为基础，
在与迁移目标最匹配的落地目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该迁移目标的版本。

## 当前迁移目标：
`reference_example/xxx`
