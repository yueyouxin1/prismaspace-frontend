# 创建
请以`doc/创建子包需求说明模板.md`为基础，
在与目标最匹配的落地组件目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该目标的版本。

## 当前目标：


### 补充规范文档：
* `packages\workflow\workflow-runtime-mock-guide.md`：Workflow 运行时契约与前端 Mock 约束
* `packages\workflow\README.md`：Workflow 子包说明

# 复刻
请以`doc/像素级复刻需求说明模板.md`为基础，
在与复刻目标最匹配的落地目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该复刻目标的版本。

## 当前复刻目标：
`reference_example/xxx`

### 补充环境规范文档：
* `packages\workflow\workflow-runtime-mock-guide.md`：Workflow 运行时契约与前端 Mock 约束

### 补充对照资源
* `packages\workflow\示例效果.jpeg`


# 迁移
请以`doc/迁移需求说明模板.md`为基础，
在与迁移目标最匹配的落地目录中创建一份需求说明文档，
并将模板内容更新为专门适用于该迁移目标的版本。

## 当前迁移目标：
`reference_example/xxx`