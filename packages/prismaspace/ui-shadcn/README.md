# @prismaspace/ui-shadcn

PrismaSpace 设计系统组件层。

## 作用

- 基于 `@prismaspace/ui-reka` 提供业务可用的 UI 组件
- 被 `studio-web` 与所有 `@prismaspace/*` workbench 共用

## 代码位置

- 组件：`src/components/ui/*`
- 工具：`src/lib/utils.ts`
