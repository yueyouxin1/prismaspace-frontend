# Param Schema Editor

Param Schema 编辑器能力（core + ui + adapters）。

- `ParamSchemaRegularEditor`：常规模式编辑器（运行态表格布局）
- `ParamSchemaProfessionalEditor`：专业模式编辑器（三栏树编辑布局）
- `useParamSchemaEditor`：运行时状态与 dispatch composable

Playground Demo: `param-schema-editor`
